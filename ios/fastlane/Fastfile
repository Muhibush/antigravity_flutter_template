default_platform(:ios)

platform :ios do
  desc "Build and upload to TestFlight"
  lane :deploy_testflight do |options|
    flavor = options[:flavor] || "prod"
    
    # 1. Fetch certificates and provisioning profiles via Match
    # match(
    #   type: "appstore",
    #   readonly: true,
    #   app_identifier: "com.example.app.#{flavor}"
    # )
    
    # 2. Increment build number automatically based on TestFlight
    # increment_build_number(
    #   build_number: latest_testflight_build_number + 1
    # )
    
    # 3. Build the app using the flavor scheme
    gym(
      scheme: flavor,
      workspace: "Runner.xcworkspace",
      clean: true,
      export_method: "app-store"
    )
    
    # 4. Upload to TestFlight
    # pilot(
    #   skip_waiting_for_build_processing: true
    # )
  end
end
